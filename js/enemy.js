export default class Enemy {
  constructor(x, y) {
    this.x = x;
    this.y = y;
    this.width = 30;
    this.height = 30;
  }

  update() {
    // Update enemy position based on input or other factors
  }

  render() {
    // Render enemy on canvas
  }
}
